% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/l2_wrappers.R
\name{GetSn}
\alias{GetSn}
\title{Estimate Noise Standard Deviation (PSD Method)}
\usage{
GetSn(y, range_ff = c(0.25, 0.5), method = c("mean", "median"))
}
\arguments{
\item{y}{Numeric vector. The observed fluorescence trace.}

\item{range_ff}{Numeric vector of length 2. Normalized frequency range [0, 0.5].}

\item{method}{Character string. Method to average the spectrum: "mean" or "median".}
}
\value{
A numeric scalar (estimated sn).
}
\description{
Estimates the noise standard deviation from the high-frequency component
of the Power Spectral Density (PSD).
}
\examples{

# 1. Simulate data with known noise (sn = 0.5)
dat <- generate_data(N = 1, sn = 0.5, seed = 123)
y <- dat$Y

# 2. Estimate noise using the PSD method
sn_est <- GetSn(y)

# 3. Check accuracy
cat(sprintf("True sn: 0.5, Estimated: \%.4f\n", sn_est))

# 4. Compare method variants
sn_mean <- GetSn(y, method = "mean")
sn_median <- GetSn(y, method = "median")

cat(sprintf("sn_mean: \%.4f\n, sn_median: \%.4f\n", sn_mean, sn_median))

}
